#pragma once
// enum of all resource types

namespace ariel{
    enum ResourceType {
        Ore = 1,
        Wheat = 2,
        Sheep = 3,
        Wood = 4,
        Brick = 5,
        Desert = 7      // not a resoure - tile type
    };
}